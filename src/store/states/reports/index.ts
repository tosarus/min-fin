import * as actions from './reportActions';
import { reducer, selectors } from './reportsReducer';
import * as sagas from './reportSagas';

export default {
  actions,
  reducer,
  sagas,
  selectors,
};
